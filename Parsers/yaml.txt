rules:
  - id: multiple-event-listeners
    languages:
      - javascript
    message: Multiple event listeners on the same element
    pattern: |
      $ELEMENT.addEventListener($EVENT, $HANDLER, ...); \n
      ... \n
      $ELEMENT.addEventListener($EVENT, $HANDLER, ...); \n
    severity: WARNING